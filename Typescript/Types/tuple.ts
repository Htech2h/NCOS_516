// number of elements is fixed
// types of element is known, and dont need to be the same

let skill:[string,number];
skill = ['htech',2];

let color:[number, number, number,string?];
color= [0,225,128];
console.log(skill);